# 開発メモ(歯科医院システム)

このファイルは、開発の進め方と進捗を記録しておくためのメモです。後で記事を書くときの材料としても使えます。

## セットアップ手順(2026-09-23)

1. ターミナルを開く
2. `cd ~/Desktop/dental-clinic-system` でプロジェクトフォルダに入る
3. `claude` と入力してClaude Codeを起動
4. フォルダを信頼するか聞かれたら、↓キーで「Yes, I trust this folder」を選んでEnter
5. `/init` と入力してEnter → `CLAUDE.md`(プロジェクトのルールを書いたファイル)が自動生成される
6. `Shift + Tab` を押してPlan Modeに切り替える
7. 次のように入力する:
   ```
   @requirements.md を読んで、この要件でどう実装を進めるか計画を立てて
   ```
8. Claude Codeが実装の計画を提示してくる → 内容を確認して、良さそうならGOを出す(実装スタート)

## 進捗ログ

- 2026-09-23: `requirements.md` 作成、`dental-clinic-system` フォルダ作成、Claude Code起動、`/init` 実行完了。次はPlan Modeでの実装計画作成へ。
- 2026-09-23: 最初の実装計画はrequirements.mdの9機能全部を含む大きい計画だった。課題のペース(課題1→課題2→最終課題)を考えて、**今回は「在庫一覧+登録+閾値アラート」の1機能だけに絞る**方針に変更。他の機能(予約管理・患者管理など)はrequirements.mdに構想として残すが、今回は実装しない。
- 2026-09-23: バックエンド方針を決定。「仮データでUIだけ先に作る」ではなく「最初からSupabaseに接続する」を選択(公開時に実データが必要なため、二度手間を避ける)。認証(ログイン)は今回は実装せず後回し。次はSupabaseで新規プロジェクトを作成する作業へ。
- 2026-09-24: Supabaseで新規プロジェクト(dental-clinic-system)を作成、consumablesテーブルを作成(RLSは暫定でallow all、ログイン実装後に修正予定)。Project URL・Publishable keyをClaude Codeに連携。在庫一覧・登録・閾値アラート機能が実装完了し、ブラウザで動作確認済み(http://localhost:3000/inventory)。Claude in Chrome拡張機能も導入し、Claude Code自身が画面を確認できるようにした。

次にやること(候補):自分でブラウザから在庫品目を2〜3個登録してみて、閾値を下回ったらアラートが出るか確認。問題なければGitHub公開・Vercelデプロイ・記事執筆へ進む。

(ここから、進んだところまで追記していきます)
